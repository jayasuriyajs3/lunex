# ===== ESP32 SERIAL BRIDGE CONFIGURATION =====

# Serial Port (COM3, COM4, etc. on Windows; /dev/ttyUSB0 on Linux)
COM_PORT=COM3

# Baud rate must match ESP32 (Serial.begin(115200))
BAUD_RATE=115200

# Backend API URL
BACKEND_URL=http://localhost:5000/api

# Machine ID (must match ESP32 firmware MACHINE_ID)
MACHINE_ID=WM-002

# Device Key (get this from Admin > Machines > Create Machine)
# ⭐ IMPORTANT: Copy the key shown after machine creation
DEVICE_KEY=esp32_WM002_your_device_key_here
